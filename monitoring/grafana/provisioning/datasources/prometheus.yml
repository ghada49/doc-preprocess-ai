apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    # PROMETHEUS_URL is injected by the ECS task definition for ECS deployments.
    url: ${PROMETHEUS_URL}
    access: proxy
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"
