global:
  resolve_timeout: 5m

route:
  group_by: ["alertname", "job"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h
  receiver: default

receivers:
  - name: default
    # No notification channel configured for local development.
    # Add Slack/PagerDuty/email receivers here for production (Phase 11).

inhibit_rules: []
