# IEP2B real-model dependencies
# Installed only when services/iep2b/Dockerfile is built with
# --build-arg ENABLE_REAL_MODEL=true.
#
# Runtime serving loads only local in-image or mounted development
# artifacts. No startup weight downloads are part of the serving path.

doclayout_yolo>=0.0.3
huggingface_hub>=0.23
numpy>=1.24.0
opencv-python-headless>=4.8.0
